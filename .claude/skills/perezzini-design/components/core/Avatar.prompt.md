**Avatar** — a circular portrait. With `ring` (default) it carries the signature 4px paper gap + 6px navy halo that lifts it off the warm page.

```jsx
<Avatar src="/imgs/profile-1.jpeg" alt="Luciano Perezzini" size={112} />
```

- `size` in px (hero uses 112); `ring={false}` for a plain circle.
